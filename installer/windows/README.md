# Windows installer

The implementation agent must choose and document the packaging approach before adding installer code.
The installer must create a desktop shortcut and Start menu entry, support clean uninstall, and keep autostart opt-in.
